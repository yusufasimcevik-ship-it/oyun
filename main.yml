name: Build APK
on: [push, workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build with Buildozer
        uses: AndreMiras/pyproject-android@v0.1.1
        with:
          project_path: .
          spec_file: buildozer.spec
